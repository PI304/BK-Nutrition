name: BK-Nutrition

on:
  push:
    branches: [main]

jobs:
  CI_CD:
    name: CI/CD
    runs-on: [self-hosted, BK-Nutrition-runner]
    steps:
      - name: Deploy
        run: |
          cd ~/BK-Nutrition
          git fetch --all && git reset --hard origin/main && git pull origin main
          npm install
          npm run build
      
